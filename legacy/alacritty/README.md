# Alacritty (⚠️ desactualizado)

> **Esta configuración está obsoleta y no se mantiene.** Es de mi antigua etapa en macOS.
> Mi terminal actual es **kitty** — ver [`kitty/`](../../kitty).

Se conserva solo como archivo histórico.
